
@page "/views/board"

@attribute [Route(Shared.Routes.Blazor.Views.BoardView)]

<!-- NOTE: CSS isolation does not work, so using inline -->
<style>

  div.color-white {
    height: 100px;
    width: 100px;
    background-color: #fff;
  }

  div.color-black {
    height: 100px;
    width: 100px;
    background-color: #000;
  }

  div.board-grid {
    height: 800px;
    width: 800px;
    box-sizing: border-box;
    border: 1px solid #000;
    margin: 0 auto;
  }

  div.board-row {
    height: 100px;
    width: 800px;
    display: flex;
  }

  b.board-columns {
  }

  p.column-letter {
  }

</style>

<div id="board-gird">
</div>

@code {

  PieceBase[] BoardPieces = Array.Empty<PieceBase>();

  [Parameter]
  public required Sets Set { get; set; }

  protected override async Task OnInitializedAsync()
  {
    StateHasChanged();
  }

}
